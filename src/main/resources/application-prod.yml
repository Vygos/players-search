spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vptpznmyptdev02p?createDatabaseIfNotExists?true
    username: ${DBUSER}
    password: ${DBPASS}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    generate-ddl: true
    hibernate:
      ddl-auto: create
  main:
    banner-mode: off
  application:
    name: @artifactId@
logging:
  file:
    name: teste
  level:
    - debug